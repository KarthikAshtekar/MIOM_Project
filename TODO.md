# MIOM Project: .ipynb in venv without full Jupyter ✅ COMPLETE

**Setup for notebook execution in VSCode (no server):**

1. `venv\\Scripts\\activate`
2. `pip install -r requirements.txt` (includes jupyter)
3. Open `notebooks/MIOM_standardized.ipynb` in VSCode
4. Select venv Python interpreter
5. Run cells with Ctrl+Enter

Jupyter deps restored, README updated for VSCode notebook execution (uses kernel, no browser/server).

Pure .ipynb runs in venv as requested.
